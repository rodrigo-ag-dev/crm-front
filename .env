# URL base da API backend, incluindo o sufixo /api.
# Em produção (front e back em domínios diferentes) isso é obrigatório.
# Em desenvolvimento local pode ser omitido (cai no fallback http://localhost:8080/api).
VITE_API_URL=https://t0cqfnbn-5035.brs.devtunnels.ms/api
